<p><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></p>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
<%= image_tag portfolio_item.thumb_image unless !portfolio_item.thumb_image %>
<p><%= link_to "Delete Item", portfolio_item, method: :delete, data: { confirm:"You sure?" } %>
<%= link_to "Edit Item", edit_portfolio_path(portfolio_item) %></p>
<hr>
